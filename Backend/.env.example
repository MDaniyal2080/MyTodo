# Backend (NestJS + Prisma) environment example for Railway + Neon

# Neon pooled connection string for application code (via PgBouncer)
# e.g. postgresql://<user>:<password>@<neon_hostname>/<database>?sslmode=require&pgbouncer=true
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require&pgbouncer=true"

# Neon direct connection string for migrations (no pooling)
# e.g. postgresql://<user>:<password>@<neon_hostname>/<database>?sslmode=require
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# Server config
PORT=3001
HOST=0.0.0.0

# CORS origins (comma-separated)
# Add your Netlify site URL(s) and any custom domains
CORS_ORIGINS=http://localhost:3000,https://your-site.netlify.app,https://your-domain.com
